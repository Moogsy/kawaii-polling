version: '3.8'
services:
  web:
    build: .
    ports:
      - "80:8000"    # Host port 80 → Container port 8000
    volumes:
      - ./:/app                    # Sync code for live updates
      - ./Pictures:/app/Pictures   # Your images folder
      - ./Ratings:/app/Ratings     # Ratings output folder
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=8jAWaIETXg0Nd8JOtIurGH_HUTsuqwFlJSLcpnJucCA

